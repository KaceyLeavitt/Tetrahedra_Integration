language: python
python:
    - "2.7"
    - "3.6"
# command to install dependencies
install:
    - pip install -- upgrade pip
    - pip install .
    - pip install tox
# command to run tests
script: tox
